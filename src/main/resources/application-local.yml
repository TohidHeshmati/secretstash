spring:
  datasource:
    url: jdbc:mysql://localhost:3106/secret_stash_local
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

  flyway:
    enabled: true
    locations: classpath:db.migrations

logging:
  level:
    org.hibernate.SQL: DEBUG